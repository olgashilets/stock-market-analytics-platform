# Data folder

## `source/`

Contains the monthly source file used to generate demonstration archive files.

## `generated_archives/`

Contains demo Excel archives with a trading-archive-like structure. These files are generated from `data/source/data.xlsx` and are used to test the ETL pipeline, raw layer, normalized layer and dashboard.

The generated archives are for demonstration and engineering validation. They are not investment recommendations.
